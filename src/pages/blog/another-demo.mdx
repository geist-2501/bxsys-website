import BlogPost from "../../components/blogPost/BlogPost.comp";
export const meta = {
  date: '4th Aug 2021'
}

export default ({children}) => <BlogPost meta={meta}>{children}</BlogPost>;

# Another test blog post.

So this is a paragraph.
And so is this.
